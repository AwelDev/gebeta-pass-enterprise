const soundCache = {}; export const playSound = (type) => { let file = ''; switch (type) { case 'click': file = './sounds/normal_click.mp3'; break; case 'signin': file = './sounds/signin_sfx.mp3'; break; case 'verified': file = './sounds/verified_sfx.mp3'; break; case 'erroreng': file = './sounds/erroreng.mp3'; break; case 'erroramh': file = './sounds/erroramh.mp3'; break; case 'errororo': file = './sounds/errororo.mp3'; break; case 'error': file = './sounds/warning.mp3'; break; case 'warning': file = './sounds/warning.mp3'; break; default: return; } try { if (!soundCache[type]) { soundCache[type] = new Audio(file); } const audio = soundCache[type]; audio.pause(); audio.currentTime = 0; const playPromise = audio.play(); if (playPromise !== undefined) { playPromise.catch(e => { console.warn("Audio play failed:", e); }); } } catch (err) { console.error("Audio System Error:", err); } };